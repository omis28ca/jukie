# Leave empty to use the same origin (recommended with the Vite dev proxy).
VITE_API_BASE=

# Optional MQTT home-automation bridge. If unset, the MQTT store stays dormant.
# VITE_MQTT_URL=ws://localhost:9001
# VITE_MQTT_TOPIC_PREFIX=jukie
