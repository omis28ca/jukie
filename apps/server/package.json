{
  "name": "office-jukebox-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:push": "prisma db push"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/multipart": "^9.0.3",
    "@prisma/client": "^6.0.0",
    "dotenv": "^17.4.2",
    "fastify": "^5.0.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
